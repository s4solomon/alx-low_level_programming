#include <stdio.h>
/**
 * main - prints alphabbets in lowercase followed bby new line
 * Return:zero is succesful
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'Z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
