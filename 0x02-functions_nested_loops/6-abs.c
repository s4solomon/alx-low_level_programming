#include "main.h"

/**
 * function: _abs - computes absolute value of an integer
 * @n: int to return as absolute
 * Return: the absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	return (-n);
}
