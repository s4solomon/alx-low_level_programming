#include "main.h"

/**
 * function: _islower - checks for lowercase
 * @c:the character to check
 * Return: 1 if c is lower case and 0 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
